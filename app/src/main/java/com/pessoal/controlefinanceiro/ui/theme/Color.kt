package com.pessoal.controlefinanceiro.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta principal — azul mais profundo e sofisticado como identidade do app
val AzulPrimario = Color(0xFF35508F)
val AzulPrimarioContainer = Color(0xFFDCE4FA)
val AzulEscuro = Color(0xFF1B2C52)

// Cor de destaque (terciária) — dourado suave, usado com moderação em
// detalhes que precisam chamar atenção sem competir com o azul principal
val DouradoDestaque = Color(0xFFB8863F)
val DouradoDestaqueContainer = Color(0xFFF6EAD2)
val DouradoDestaqueEscuro = Color(0xFF4A3A17)

// Verde/vermelho — mesma paleta já usada nos indicadores de Entrada/Saída
val VerdeEntrada = Color(0xFF2E7D32)
val VerdeEntradaContainer = Color(0xFFD9F0DA)
val VermelhoSaida = Color(0xFFC62828)
val VermelhoSaidaContainer = Color(0xFFF9DCDC)

// Neutros — fundo levemente frio, com uma camada extra de superfície pra dar
// profundidade aos cards e separar visualmente o conteúdo do fundo
val FundoApp = Color(0xFFF3F5FA)
val SuperficieCard = Color(0xFFFFFFFF)
val SuperficieAlta = Color(0xFFEBEFF9)

// Contorno mais escuro — usado na borda dos campos de texto e seleção (não
// focados), pra ficarem bem visíveis mesmo sem estar em foco
val Contorno = Color(0xFF8B93AC)
// Contorno sutil — usado só em divisores de tela, mais discreto
val ContornoSutil = Color(0xFFDBE1EF)
// Contorno do divisor — mais escuro que o ContornoSutil, pra marcar melhor
// a separação entre o cabeçalho/filtros e o conteúdo das telas
val ContornoDivider = Color(0xFF9AA2BC)

val TextoPrincipal = Color(0xFF1B1E28)
val TextoSecundario = Color(0xFF5B6072)
